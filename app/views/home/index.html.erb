<h1><%= t('home.welcome') %></h1>

<% if user_signed_in? %>
  <p><%= t('home.greeting', email: current_user.email) %></p>
  <!-- Se está generando un extraño error de ruteo que no permite cerrar sesión --->
  <!-- Estoy mandando un delete pero se está procesando como un GET -->
  <%= button_to t('home.sign_out'), destroy_user_session_path, method: :delete %>
<% else %>
  <%= link_to t('home.sign_in'), new_user_session_path %>
  <%= link_to t('home.sign_up'), new_user_registration_path %>
<% end %>
