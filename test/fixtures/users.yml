one:
  email: user@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

two:
  email: another_user@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'another_password') %>

